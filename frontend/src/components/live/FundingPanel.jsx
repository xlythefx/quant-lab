import Panel from "./Panel.jsx";

/** Funding & Open Interest — SIMULATED placeholder, fleshed out in phase 08. */
export default function FundingPanel({ symbol }) {
  return (
    <Panel title="Funding & Open Interest" simulated>
      <div className="lt-empty">Funding / OI panel — built in phase 08</div>
    </Panel>
  );
}
