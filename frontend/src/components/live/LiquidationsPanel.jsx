import Panel from "./Panel.jsx";

/** Liquidations — SIMULATED placeholder, fleshed out in phase 08. */
export default function LiquidationsPanel({ symbol, lastPrice }) {
  return (
    <Panel title="Liquidations" simulated>
      <div className="lt-empty">Streaming liquidation feed — built in phase 08</div>
    </Panel>
  );
}
