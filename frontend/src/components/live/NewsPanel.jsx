import Panel from "./Panel.jsx";

/** News feed — SIMULATED placeholder, fleshed out in phase 08. */
export default function NewsPanel() {
  return (
    <Panel title="News Feed" simulated>
      <div className="lt-empty">Headline marquee — built in phase 08</div>
    </Panel>
  );
}
