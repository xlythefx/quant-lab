---
tags: [page]
type: page
---

# Dashboard V1

`frontend/src/pages/Dashboard.jsx` — the original dashboard. Also runs through [[Portfolio Runner]] (same engine/stats as [[Dashboard V2]]), so the numbers match. Uses the shared `TradingChart` (which already renders the [[Regime Detection]] ribbon) and `StatsPanel` / `PortfolioSummary`.

Mostly superseded by [[Dashboard V2]]; kept for reference. Surfaces a few things V2 doesn't (per-side Long/Short detail, skipped-signal counterfactuals) — though those also live in [[Analytics]].

Related: [[Dashboard V2]] · [[Analytics]]
