backend/.venv/
backend/data/*.parquet
backend/data/**/*.parquet
backend/__pycache__/
backend/**/__pycache__/
frontend/node_modules/
frontend/dist/
.DS_Store
*.log
backend/.env
.env
.claude/

# Local scratch files (contain VPS credentials — never commit)
_vps_*.py
_bar_exporter.txt

# Reference copies pulled from external machines (may contain .env / tokens)
vendor/

# Raw broker/strategy CSV data — kept local
backend/data/tradestation/

# Runtime webhook config — contains live URLs and secrets
backend/data/live_alerts.json

# VPS deployment scripts and configs (may contain credentials/IPs)
vps-deployment/
nginx_eu_current.conf

# External data pulled from VPS / other machines
external-data/
