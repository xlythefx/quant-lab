# Isolated dependencies for the HMM regime experiment.
# Deliberately kept OUT of backend/requirements.txt so this experiment cannot
# affect the production app. Install with:
#     pip install -r experiments/hmm_regime/requirements-hmm.txt
#
# The Gaussian HMM is implemented from scratch in hmm_model.py (pure numpy/scipy),
# so there is NO hmmlearn dependency — hmmlearn has no prebuilt wheel for Python
# 3.14 and would require a C++ toolchain to compile. numpy / scipy / scikit-learn
# are already provided by the backend env; only matplotlib (for the plots) is new.
matplotlib>=3.8.0
